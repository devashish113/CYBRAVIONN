# Production Environment Template
# Note: Never expose private API keys (Gemini, OpenAI, AWS) in client-side bundles.
# All AI / LLM calls must be proxied through secure backend services (/api/*).

# Server / App URL
APP_URL="https://cybravions.com"
